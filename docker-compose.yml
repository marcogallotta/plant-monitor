services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./data/photos:/app/data/photos
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  pi:
    build: ./pi
    volumes:
      - ./pi:/app
